import React from 'react';

const Select = (props) => {
    const {name, value, label, onChange, option} = props
    return (
        <div>
            
        </div>
    );
};

export default Select;